# Defaults for kconfig-frontends initscript
# sourced by /etc/init.d/kconfig-frontends
# installed at /etc/default/kconfig-frontends by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
